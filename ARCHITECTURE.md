# Архитектура проекта "Строительные калькуляторы"

## 📐 Обзор архитектуры

Приложение построено на принципах **Clean Architecture** с разделением на слои и использованием **MVVM** паттерна для управления состоянием.

## 🏗️ Слои приложения

### 1. Domain Layer (Доменный слой)

**Назначение:** Содержит бизнес-логику, модели данных и правила расчётов.

**Компоненты:**

#### `domain/engine/`
- **CalculatorEngine.kt** - Центральный движок расчётов
  - 21 функция расчёта (по одной на калькулятор)
  - Функции генерации подробных описаний
  - Валидация через InputValidator
  - Обработка ошибок через CalculationResult
  
- **InputValidator.kt** - Валидатор входных данных
  - Проверка обязательных полей
  - Валидация типов (LENGTH, AREA, VOLUME, etc.)
  - Проверка диапазонов (min/max)
  - Тип-специфичные правила
  
- **CalculationResult.kt** - Результаты расчётов
  - Sealed class: Success<T> / Error
  - Типизированные ошибки (MISSING_INPUT, VALIDATION, INTERNAL)
  - Пользовательские сообщения об ошибках

#### `domain/model/`
- **CalculatorDefinition.kt** - Полное определение калькулятора
- **InputFieldDefinition.kt** - Определение поля ввода
- **ResultFieldDefinition.kt** - Определение поля результата
- **CalculatorCategory.kt** - Категория калькуляторов
- **UsageExample.kt** - Пример использования
- **InputFieldType.kt** - Enum типов полей ввода

#### `domain/repository/`
- **CalculatorRepository.kt** - Репозиторий всех калькуляторов
  - Статический in-memory репозиторий
  - 21 калькулятор в 4 категориях
  - Методы поиска и фильтрации
  - Все данные основаны на calc1.ru

### 2. UI Layer (Слой представления)

**Назначение:** Отвечает за отображение данных и взаимодействие с пользователем.

**Технологии:** Jetpack Compose, Material Design 3

#### `ui/calculator/`
- **CalculatorScreen.kt** - Экран калькулятора
  - Отображение полей ввода
  - Отображение результатов
  - Условное отображение подробных описаний (премиум)
  - Кнопки шаринга
  
- **CalculatorViewModel.kt** - ViewModel для калькулятора
  - Управление состоянием через StateFlow
  - Валидация входных данных
  - Вызов CalculatorEngine для расчётов
  - Управление ошибками
  
- **CalculatorViewModelFactory.kt** - Фабрика ViewModel
  - Создание ViewModel с параметрами (calculatorId)
  
- **ShareTextBuilder.kt** - Построитель текста для шаринга
  - Форматирование результатов для шаринга

#### `ui/home/`
- **HomeScreen.kt** - Главный экран
  - Поиск калькуляторов
  - Популярные калькуляторы
  - Секция категорий
  
- **PopularSection.kt** - Секция популярных калькуляторов
- **CategorySection.kt** - Секция категорий
- **CalculatorList.kt** - Список калькуляторов

#### `ui/category/`
- **CategoryScreen.kt** - Экран категории
  - Список калькуляторов в категории

#### `ui/history/`
- **HistoryScreen.kt** - Экран истории
  - Список сохранённых расчётов
  - Возможность шаринга из истории
  
- **HistoryViewModel.kt** - ViewModel для истории
  - Управление историей через SharedPreferences
  - JSON сериализация/десериализация

#### `ui/about/`
- **AboutScreen.kt** - Экран информации о приложении
  - Информация о приложении
  - Кнопка шаринга приложения

#### `ui/theme/`
- **Theme.kt** - Тема приложения
  - Material Design 3 цветовая схема
  - Типографика
  - Поддержка тёмной темы

### 3. Navigation Layer (Слой навигации)

**Назначение:** Управление навигацией между экранами.

- **NavGraph.kt** - Граф навигации
  - Определение всех маршрутов
  - Параметры навигации
  - Обработка back stack
  
- **NavRoutes.kt** - Константы маршрутов
  - HOME, CATEGORY, CALCULATOR
  - Функции построения маршрутов с параметрами

### 4. Config Layer (Слой конфигурации)

**Назначение:** Глобальная конфигурация приложения.

- **AppConfig.kt** - Конфигурационные флаги
  - premiumEnabled: Boolean - Флаг премиум-функций
  - Готово к интеграции с RuStore Billing

### 5. Util Layer (Слой утилит)

**Назначение:** Вспомогательные функции.

- **ShareHelper.kt** - Помощник для шаринга
  - Обёртка над Android Intent.ACTION_SEND
  - Открытие системного диалога выбора приложения

## 🔄 Поток данных

### Расчёт калькулятора:

```
User Input (UI)
    ↓
CalculatorViewModel.updateInput()
    ↓
CalculatorViewModel.calculate()
    ↓
InputValidator.validateInputs()
    ↓
CalculatorEngine.calculate()
    ↓
CalculatorEngine.getCalculationDetails() (всегда вычисляется)
    ↓
CalculatorViewModel._results.value = results
CalculatorViewModel._calculationDetails.value = details
    ↓
UI recomposes (StateFlow.collectAsState())
    ↓
Results displayed (basic always, details if premiumEnabled)
```

### Навигация:

```
HomeScreen
    ↓ (onCategoryClick)
CategoryScreen
    ↓ (onCalculatorClick)
CalculatorScreen
    ↓ (onNavigateUp)
Back to previous screen
```

## 📦 Зависимости между слоями

```
UI Layer
    ↓ depends on
Domain Layer (models, engine, repository)
    ↓ depends on
Android SDK (Context, Intent, SharedPreferences)
```

**Правила:**
- UI Layer зависит от Domain Layer
- Domain Layer не зависит от UI Layer
- Config и Util - независимые утилиты

## 🎯 Паттерны проектирования

### 1. Repository Pattern
- **CalculatorRepository** - единая точка доступа к данным калькуляторов
- Инкапсулирует логику получения данных
- Легко заменить на базу данных в будущем

### 2. MVVM Pattern
- **ViewModel** - управление состоянием
- **StateFlow** - реактивные потоки данных
- **Compose UI** - декларативный UI

### 3. Factory Pattern
- **CalculatorViewModelFactory** - создание ViewModel с параметрами
- Позволяет передавать calculatorId в конструктор

### 4. Singleton Pattern
- **CalculatorEngine** - один экземпляр для всех расчётов
- **InputValidator** - один экземпляр валидатора
- **AppConfig** - глобальная конфигурация
- **CalculatorRepository** - единый репозиторий

### 5. Strategy Pattern
- Разные алгоритмы расчёта для разных калькуляторов
- when-выражение в CalculatorEngine выбирает стратегию

### 6. Sealed Classes
- **CalculationResult** - Success/Error варианты
- Type-safe обработка результатов

## 🔐 Управление состоянием

### StateFlow Pattern

```kotlin
// Приватное изменяемое состояние
private val _state = MutableStateFlow<State>(initialState)

// Публичное неизменяемое состояние
val state: StateFlow<State> = _state.asStateFlow()

// В Compose
val state by viewModel.state.collectAsState()
```

### Преимущества:
- Автоматическая рекомпозиция UI при изменении состояния
- Сохранение состояния при повороте экрана
- Типобезопасность
- Реактивное программирование

## 🧪 Тестирование

### Unit Tests
- `CalculatorEngineSmokeTest.kt` - Smoke tests для всех калькуляторов
- `WallpaperCalculatorTest.kt` - Unit tests для конкретного калькулятора

### Тестируемые компоненты:
- CalculatorEngine - логика расчётов
- InputValidator - валидация входных данных
- CalculatorRepository - получение данных

## 🚀 Производительность

### Оптимизации:
- In-memory репозиторий (быстрый доступ)
- Stateless вычисления (нет побочных эффектов)
- StateFlow (эффективные обновления UI)
- LazyColumn для списков (ленивая загрузка)

### Ограничения:
- SharedPreferences для истории (подходит для небольших объёмов)
- Синхронные расчёты (быстрые, но блокируют UI поток)
- Нет кэширования (всегда пересчитывается)

## 🔮 Будущие улучшения

### Архитектурные:
- Миграция истории на Room database
- Асинхронные расчёты для сложных калькуляторов
- Кэширование результатов
- Dependency Injection (Hilt/Koin)

### Функциональные:
- Офлайн-режим
- Синхронизация между устройствами
- Облачное хранение истории
- Экспорт в PDF

## 📝 Соглашения о коде

### Именование:
- Классы: PascalCase (CalculatorEngine)
- Функции: camelCase (calculateWallpaper)
- Переменные: camelCase (roomLength)
- Константы: UPPER_SNAKE_CASE (MAX_LENGTH)
- Пакеты: lowercase (ru.calc1.construction.domain)

### Структура файлов:
- Один класс/объект на файл
- Логическая группировка по пакетам
- Комментарии на английском языке

### Комментарии:
- KDoc для публичных API
- Inline комментарии для сложной логики
- TODO для будущих улучшений



