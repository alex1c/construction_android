# Строительные калькуляторы Calc1

Android приложение для расчёта строительных материалов и конструкций. Содержит 21 профессиональный калькулятор для различных строительных задач.

## 📱 Описание

Приложение предоставляет набор строительных калькуляторов для точного расчёта материалов и конструкций. Все расчёты основаны на стандартах ГОСТ и соответствуют калькуляторам на сайте [calc1.ru](https://calc1.ru/ru/construction).

### Основные возможности

- **21 калькулятор** в 4 категориях
- **Точные расчёты** на основе ГОСТ и строительных стандартов
- **Поиск калькуляторов** по названию и описанию
- **История расчётов** с возможностью повторного использования
- **Поделиться результатами** через мессенджеры и социальные сети
- **Подробные описания расчётов** (премиум-функция, готово к активации)
- **Примеры использования** для каждого калькулятора

## 🧮 Калькуляторы

### Отделка и интерьер (9 калькуляторов)
1. **Калькулятор обоев** - Расчёт количества рулонов обоев
2. **Калькулятор краски** - Расчёт количества краски для стен
3. **Калькулятор плиточного клея** - Расчёт клея для укладки плитки
4. **Калькулятор шпатлёвки** - Расчёт шпатлёвки для выравнивания стен
5. **Калькулятор грунтовки** - Расчёт грунтовки для подготовки поверхности
6. **Калькулятор штукатурки** - Расчёт штукатурки для стен
7. **Калькулятор площади стен** - Расчёт площади стен с учётом проёмов
8. **Калькулятор плитки** - Расчёт количества плитки для пола и стен
9. **Калькулятор ламината** - Расчёт количества ламината для пола

### Конструкции и бетон (6 калькуляторов)
10. **Калькулятор фундамента** - Расчёт материалов для фундамента (бетон, арматура, опалубка)
11. **Калькулятор бетона** - Расчёт количества цемента, песка, щебня и воды
12. **Калькулятор кровли** - Расчёт площади крыши и количества кровельных материалов
13. **Калькулятор кирпича и блоков** - Расчёт количества кирпичей, газоблоков и пеноблоков
14. **Калькулятор лестниц** - Расчёт угла наклона, количества ступеней и длины пролёта
15. **Калькулятор щебня** - Расчёт количества щебня для фундамента, дорожек и подсыпки

### Инженерные системы (3 калькулятора)
16. **Калькулятор вентиляции** - Расчёт производительности вентиляции
17. **Калькулятор тёплого пола** - Расчёт мощности и энергопотребления тёплого пола
18. **Калькулятор водопроводных труб** - Расчёт диаметра, расхода и потерь давления

### Металл и электрика (3 калькулятора)
19. **Калькулятор арматуры** - Расчёт количества и массы арматуры для различных конструкций
20. **Калькулятор сечения кабеля** - Расчёт сечения кабеля по мощности и току
21. **Калькулятор электрики** - Расчёт тока, сечения кабеля и номинала автомата

## 🏗️ Архитектура проекта

Приложение построено на принципах Clean Architecture с разделением на слои:

```
app/src/main/java/com/construction/
├── config/                    # Конфигурация приложения
│   └── AppConfig.kt          # Глобальные флаги (премиум, feature flags)
│
├── domain/                    # Доменный слой (бизнес-логика)
│   ├── engine/               # Движок расчётов
│   │   ├── CalculatorEngine.kt      # Основной движок расчётов (21 калькулятор)
│   │   ├── InputValidator.kt        # Валидация входных данных
│   │   └── CalculationResult.kt     # Результаты расчётов (Success/Error)
│   │
│   ├── model/                # Модели данных
│   │   ├── CalculatorDefinition.kt  # Определение калькулятора
│   │   ├── InputFieldDefinition.kt  # Определение поля ввода
│   │   ├── ResultFieldDefinition.kt # Определение поля результата
│   │   ├── CalculatorCategory.kt    # Категория калькуляторов
│   │   └── UsageExample.kt          # Пример использования
│   │
│   └── repository/           # Репозиторий данных
│       └── CalculatorRepository.kt  # Репозиторий всех калькуляторов
│
├── navigation/               # Навигация
│   ├── NavGraph.kt          # Граф навигации
│   └── NavRoutes.kt         # Маршруты навигации
│
├── ui/                       # UI слой (Jetpack Compose)
│   ├── calculator/          # Экран калькулятора
│   │   ├── CalculatorScreen.kt      # UI экрана калькулятора
│   │   ├── CalculatorViewModel.kt   # ViewModel для калькулятора
│   │   ├── CalculatorViewModelFactory.kt  # Фабрика ViewModel
│   │   └── ShareTextBuilder.kt     # Построитель текста для шаринга
│   │
│   ├── home/                # Главный экран
│   │   ├── HomeScreen.kt           # Главный экран
│   │   ├── PopularSection.kt       # Секция популярных калькуляторов
│   │   ├── CategorySection.kt      # Секция категорий
│   │   └── CalculatorList.kt      # Список калькуляторов
│   │
│   ├── category/            # Экран категории
│   │   └── CategoryScreen.kt       # Экран списка калькуляторов в категории
│   │
│   ├── history/             # История расчётов
│   │   ├── HistoryScreen.kt        # Экран истории
│   │   └── HistoryViewModel.kt     # ViewModel для истории
│   │
│   ├── about/               # О приложении
│   │   └── AboutScreen.kt          # Экран информации о приложении
│   │
│   └── theme/               # Тема приложения
│       └── Theme.kt                 # Material Design 3 тема
│
├── util/                     # Утилиты
│   └── ShareHelper.kt       # Помощник для шаринга текста
│
└── MainActivity.kt          # Главная активность
```

## 🛠️ Технологии

### Основные технологии
- **Kotlin** - Язык программирования
- **Jetpack Compose** - Современный UI фреймворк
- **Material Design 3** - Дизайн-система
- **Navigation Component** - Навигация между экранами
- **ViewModel** - Управление состоянием UI
- **StateFlow** - Реактивные потоки данных
- **Coroutines** - Асинхронное программирование

### Зависимости
```gradle
// Core Android
androidx.core:core-ktx:1.12.0
androidx.lifecycle:lifecycle-runtime-ktx:2.6.2
androidx.activity:activity-compose:1.8.1

// Compose
androidx.compose:compose-bom:2023.10.01
androidx.compose.ui:ui
androidx.compose.material3:material3

// Navigation
androidx.navigation:navigation-compose:2.7.5

// ViewModel
androidx.lifecycle:lifecycle-viewmodel-compose:2.6.2
```

## 📋 Требования

- **Android Studio** Hedgehog (2023.1.1) или новее
- **JDK 17** или новее
- **Android SDK 34**
- **Min SDK: 24** (Android 7.0 Nougat)
- **Target SDK: 34** (Android 14)

## 🚀 Запуск проекта

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/alex1c/construction_android.git
   cd construction_android
   ```

2. **Откройте проект в Android Studio**
   - File > Open > выберите папку проекта
   - Дождитесь синхронизации Gradle

3. **Подключите устройство или запустите эмулятор**
   - Минимальная версия Android: 7.0 (API 24)

4. **Запустите приложение**
   - Нажмите Run (Shift+F10) или
   - Выберите Run > Run 'app' в меню

## 📐 Принципы расчётов

Все калькуляторы используют проверенные формулы и стандарты:

- **ГОСТ** - Государственные стандарты для строительных материалов
- **СНиП** - Строительные нормы и правила
- **Международные стандарты** - Для электрических и инженерных расчётов

### Примеры формул:

**Калькулятор обоев:**
```
Площадь стен = Периметр × Высота - Площадь проёмов
Количество рулонов = ⌈(Площадь стен × (1 + Запас%)) / Площадь рулона⌉
```

**Калькулятор бетона:**
```
Компонент = Объём × Плотность × Пропорция / Сумма пропорций
Вода = Цемент × Водоцементное соотношение
```

**Калькулятор арматуры:**
```
Количество стержней = ⌈Длина / Шаг сетки + 1⌉
Общая длина = Количество × Длина стержня
Масса = Длина × Масса на метр (по ГОСТ)
```

**Калькулятор электрики:**
```
Ток = Мощность / (Напряжение × √3 × cos φ)  // для трёхфазной сети
Сечение = Ток / (Плотность тока × Коэффициент температуры)
Номинал автомата = Ток × 1.25 (запас 25%)
```

## 💻 Принципы разработки

### Архитектурные принципы

1. **Clean Architecture**
   - Разделение на слои: Domain, UI, Data
   - Независимость бизнес-логики от UI
   - Легкое тестирование и поддержка

2. **MVVM Pattern**
   - ViewModel управляет состоянием
   - StateFlow для реактивных обновлений UI
   - Разделение ответственности

3. **Single Activity**
   - Все экраны - Compose функции
   - Навигация через Navigation Component
   - Упрощённое управление жизненным циклом

4. **Stateless Components**
   - Чистые функции для расчётов
   - Нет побочных эффектов
   - Предсказуемое поведение

### Паттерны проектирования

- **Repository Pattern**: Единая точка доступа к данным
- **Factory Pattern**: Создание ViewModel через фабрику
- **Singleton Pattern**: AppConfig, CalculatorEngine, InputValidator
- **Strategy Pattern**: Разные алгоритмы расчёта для разных калькуляторов

### Управление состоянием

- **StateFlow**: Реактивные потоки данных
- **MutableStateFlow**: Приватные изменяемые состояния
- **asStateFlow()**: Публичные неизменяемые состояния
- **collectAsState()**: Подписка в Compose

### Обработка ошибок

- **CalculationResult**: Sealed class для Success/Error
- **Валидация входных данных**: InputValidator
- **Пользовательские сообщения**: Понятные ошибки на русском
- **Graceful degradation**: Приложение продолжает работать при ошибках

## 🔧 Ключевые компоненты

### CalculatorEngine
Центральный движок расчётов. Содержит:
- 21 функцию расчёта (по одной на калькулятор)
- Функции генерации подробных описаний
- Валидацию через InputValidator
- Обработку ошибок

### CalculatorRepository
Репозиторий всех калькуляторов. Содержит:
- Определения всех 21 калькулятора
- Метаданные (поля ввода, результаты, примеры)
- Категории калькуляторов
- Методы поиска и фильтрации

### InputValidator
Валидатор входных данных. Проверяет:
- Наличие обязательных полей
- Корректность числовых значений
- Диапазоны значений (min/max)
- Тип-специфичные правила

### AppConfig
Глобальная конфигурация. Управляет:
- Премиум-функциями (premiumEnabled)
- Feature flags
- Готово к интеграции с RuStore Billing

## 📊 Структура данных

### CalculatorDefinition
Полное определение калькулятора:
```kotlin
data class CalculatorDefinition(
    val id: String,                    // Уникальный ID
    val categoryId: String,            // ID категории
    val name: String,                  // Название
    val shortDescription: String,      // Описание
    val inputFields: List<InputFieldDefinition>,  // Поля ввода
    val resultFields: List<ResultFieldDefinition>, // Поля результатов
    val helpText: String? = null,      // Подсказка
    val usageExamples: List<UsageExample> = emptyList()  // Примеры
)
```

### InputFieldDefinition
Определение поля ввода:
```kotlin
data class InputFieldDefinition(
    val id: String,                    // ID поля
    val label: String,                  // Название
    val unit: String? = null,          // Единица измерения
    val type: InputFieldType,          // Тип (NUMBER, LENGTH, DROPDOWN, etc.)
    val hint: String? = null,          // Подсказка
    val defaultValue: Double? = null,  // Значение по умолчанию
    val options: List<Pair<Double, String>>? = null  // Опции для dropdown
)
```

### InputFieldType
Типы полей ввода:
- `NUMBER` - Общее числовое поле
- `LENGTH` - Длина (м, см, мм)
- `AREA` - Площадь (м²)
- `VOLUME` - Объём (м³)
- `MASS` - Масса (кг)
- `PERCENT` - Процент (0-100)
- `POWER` - Мощность (Вт, кВт)
- `FLOW` - Расход (м³/ч, л/с)
- `INTEGER` - Целое число
- `DROPDOWN` - Выпадающий список

## 🎯 Особенности реализации

### Выпадающие списки
Многие калькуляторы используют dropdown для удобства:
- Тип фундамента, бетона, кровли
- Тип материала (кирпич, блоки)
- Тип лестницы, работ, фракции
- Параметры электрики и кабелей

### Подробные описания расчётов
Все калькуляторы имеют функции генерации подробных описаний:
- Пошаговые расчёты с формулами
- Промежуточные значения
- Рекомендации по использованию
- Скрыты за флагом `AppConfig.premiumEnabled`

### История расчётов
- Хранение в SharedPreferences (без базы данных)
- JSON сериализация/десериализация
- Сохранение входных параметров и результатов
- Возможность поделиться из истории

### Шаринг результатов
- Форматированный текст с параметрами и результатами
- Поддержка всех мессенджеров и приложений
- Системный диалог выбора приложения
- Ярко-зелёные кнопки шаринга

## 🧪 Тестирование

Проект включает unit-тесты для проверки корректности расчётов:

```bash
./gradlew test
```

Тесты находятся в `app/src/test/java/com/construction/`

### Покрытие тестами:
- Smoke tests для всех калькуляторов
- Unit tests для конкретных калькуляторов
- Валидация входных данных
- Проверка формул и результатов

## 🎨 Особенности UI

- **Material Design 3** - Современный дизайн с поддержкой темной темы
- **Адаптивный дизайн** - Работает на телефонах и планшетах
- **Поиск** - Быстрый поиск калькуляторов по названию
- **Категории** - Удобная организация калькуляторов
- **Примеры** - Практические примеры для каждого калькулятора
- **История** - Сохранение результатов расчётов

## 💎 Премиум функции

Приложение готово к интеграции премиум-функций через RuStore Billing:

- **Подробные описания расчётов** - Пошаговые объяснения всех формул
- **Расширенная история** - Неограниченное количество сохранённых расчётов
- **Экспорт результатов** - Экспорт в PDF и другие форматы

Премиум-функции скрыты за флагом `AppConfig.premiumEnabled` и готовы к активации.

## 📦 Структура данных

### CalculatorDefinition
```kotlin
data class CalculatorDefinition(
    val id: String,                    // Уникальный ID калькулятора
    val categoryId: String,            // ID категории
    val name: String,                  // Название калькулятора
    val shortDescription: String,      // Краткое описание
    val inputFields: List<InputFieldDefinition>,  // Поля ввода
    val resultFields: List<ResultFieldDefinition>, // Поля результатов
    val helpText: String? = null,       // Подсказка
    val usageExamples: List<UsageExample> = emptyList()  // Примеры
)
```

### InputFieldDefinition
```kotlin
data class InputFieldDefinition(
    val id: String,                    // ID поля
    val label: String,                  // Название поля
    val unit: String? = null,          // Единица измерения
    val type: InputFieldType,          // Тип поля (NUMBER, LENGTH, DROPDOWN, etc.)
    val hint: String? = null,          // Подсказка
    val defaultValue: Double? = null,  // Значение по умолчанию
    val options: List<Pair<Double, String>>? = null  // Опции для dropdown
)
```

## 🧪 Тестирование

Проект включает unit-тесты для проверки корректности расчётов:

```bash
./gradlew test
```

Тесты находятся в `app/src/test/java/com/construction/`

## 📝 Лицензия

MIT License

## 👨‍💻 Разработка

### Добавление нового калькулятора

1. **Добавьте определение в CalculatorRepository.kt:**
   ```kotlin
   private fun createNewCalculator(): CalculatorDefinition {
       return CalculatorDefinition(
           id = "new_calculator",
           categoryId = CATEGORY_FINISHING,
           name = "Новый калькулятор",
           shortDescription = "Описание калькулятора",
           inputFields = listOf(
               InputFieldDefinition(
                   id = "input_field",
                   label = "Название поля",
                   unit = "м",
                   type = InputFieldType.LENGTH,
                   hint = "Подсказка"
               )
           ),
           resultFields = listOf(
               ResultFieldDefinition(
                   id = "result_field",
                   label = "Результат",
                   unit = "шт"
               ),
               ResultFieldDefinition(
                   id = "calculation_details",
                   label = "Подробности расчёта",
                   unit = null
               )
           ),
           usageExamples = listOf(/* примеры */)
       )
   }
   ```

2. **Добавьте в список калькуляторов:**
   ```kotlin
   fun getCalculators(): List<CalculatorDefinition> {
       return listOf(
           // ...
           createNewCalculator(),
       )
   }
   ```

3. **Добавьте функцию расчёта в CalculatorEngine.kt:**
   ```kotlin
   private fun calculateNewCalculator(inputs: Map<String, Double>): Map<String, Double> {
       val inputValue = inputs["input_field"] ?: 0.0
       val result = inputValue * 2.0 // Ваша формула
       return mapOf("result_field" to result)
   }
   ```

4. **Добавьте в when-выражение:**
   ```kotlin
   "new_calculator" -> calculateNewCalculator(inputs)
   ```

5. **Добавьте функцию подробного описания (опционально):**
   ```kotlin
   private fun getNewCalculatorCalculationDetails(inputs: Map<String, Double>): String {
       return buildString {
           appendLine("ПОДРОБНЫЙ РАСЧЁТ")
           // Ваше описание
       }
   }
   ```

6. **Добавьте в getCalculationDetails:**
   ```kotlin
   "new_calculator" -> getNewCalculatorCalculationDetails(inputs)
   ```

### Изменение темы

Тема приложения настраивается в `app/src/main/java/com/construction/ui/theme/Theme.kt`

### Добавление нового типа поля

1. Добавьте значение в `InputFieldType` enum
2. Добавьте валидацию в `InputValidator.validateInput()`
3. Обновите UI в `CalculatorScreen.InputField()` при необходимости

## 🔄 Версионирование

### Версия 1.0 (Текущая)
- ✅ 21 калькулятор
- ✅ Поиск калькуляторов
- ✅ История расчётов
- ✅ Шаринг результатов
- ✅ Примеры использования
- ⏳ Подробные описания (скрыты, готовы к активации)

### Планы на будущее

#### Версия 1.1
- [ ] Интеграция RuStore Billing
- [ ] Активация премиум-функций
- [ ] Экспорт результатов в PDF
- [ ] Расширенная история для премиум-пользователей

#### Версия 1.2
- [ ] Сохранение избранных калькуляторов
- [ ] Персонализированные рекомендации
- [ ] Улучшенный поиск с фильтрами
- [ ] Темная тема (улучшения)

#### Версия 2.0
- [ ] Офлайн-режим с кэшированием
- [ ] Синхронизация между устройствами
- [ ] Облачное хранение истории
- [ ] Дополнительные калькуляторы

## 🐛 Известные ограничения

- История хранится в SharedPreferences (подходит для небольших объёмов)
- Подробные описания скрыты до активации премиума
- Нет офлайн-режима (все данные локальные)
- Нет синхронизации между устройствами

## 🔗 Ссылки

- **Веб-версия калькуляторов:** [calc1.ru/ru/construction](https://calc1.ru/ru/construction)
- **Репозиторий:** [GitHub](https://github.com/alex1c/construction_android)

## 📞 Контакты

Для вопросов и предложений создайте issue в репозитории.

## 🙏 Благодарности

- Калькуляторы основаны на спецификациях [calc1.ru](https://calc1.ru)
- Формулы соответствуют ГОСТ и СНиП
- Используются стандарты Material Design 3

---

**Версия:** 1.0  
**Дата релиза:** 2024  
**Платформа:** Android 7.0+ (API 24+)  
**Минимальная версия:** Android 7.0 Nougat (API 24)  
**Целевая версия:** Android 14 (API 34)
